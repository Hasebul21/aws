Option A: The "Developer" Way (Fastest)
Use this when you want to see code changes instantly (Hot Module Replacement).

Start Database: docker-compose up -d db

Start Backend: Inside /backend, run npm install then npm start.

Start Frontend: Inside /frontend, run npm install then npm run dev.

Visit: http://localhost:5173